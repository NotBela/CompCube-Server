# Contributing
Prerequisites:
- Docker
- IIS Express

1. Clone this repo
2. Open a terminal window and set working directory to cloned git repo
3. `docker pull container-registry.oracle.com/mysql/community-server`
4. `docker run --name CompCubeDB --restart on-failure -d container-registry.oracle.com/mysql/community-server:latest`
5. 
